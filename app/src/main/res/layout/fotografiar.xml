<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.camera.view.PreviewView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/previewFotografiar"
        />

    <Button
        android:id="@+id/buttonFotografiar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:text="Capturar"
        app:layout_constraintBottom_toBottomOf="@+id/previewFotografiar"
        app:layout_constraintEnd_toStartOf="@+id/buttonCancelarFotografiar"
        app:layout_constraintStart_toStartOf="@+id/previewFotografiar" />

    <Button
        android:id="@+id/buttonCancelarFotografiar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:text="Cancelar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/buttonFotografiar" />

</androidx.constraintlayout.widget.ConstraintLayout>